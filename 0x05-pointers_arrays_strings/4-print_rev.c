#includec"main.h"
/**
 * print_rev -imprime en reversa
 * @s: string
 * Return: 0
 */
void print_rev(char *s)
{
	int longi = 0;
	int o;

	while (*s != '\0')
	{
		long++;
		s++;
	}
	S--;
	for (o = longi; o > 0; O--)
	{
		putchar(*s);
		S--;
	}
	putchar('\n');
}
